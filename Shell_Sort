void ShellSort (int arr[], int n) 
{
  int i, j, h;
  for (h=n/2; h>0; h/=2) 
  {
    for (i=h; i<n; i++) 
    {
      int x=arr[i];
      for (j=i; j>=h && x<arr[j-h]; j-=h) 
        arr[j]=arr[j-h];
      arr[j]=x;
    }
  }
}

//in this case int but it can sort other types as well
